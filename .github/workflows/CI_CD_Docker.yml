name: CI_CD_Docker
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
#This job is to build the Docker file
  build_docker:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3    
      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
      -
        name: Build and push
        uses: docker/build-push-action@v4
        with:
          push: true
          tags: chphanisaisriram/mlops:ci_cd_v1
  #This job is to pull the Docker image from the above and run the test.py script
  pull_docker:
    
    if: ${{ always() }}
    needs: [build_docker]
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Log in to registry
      uses: docker/login-action@v2
      with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
    - name: pull Docker Image
      run: docker image pull chphanisaisriram/mlops:ci_cd_v1
    - name: run the image
      run: |
        OUTPUT=$(docker run chphanisaisriram/mlops:ci_cd_v1)
        echo "Score:"
        echo "$OUTPUT"
        if [[ `echo "$OUTPUT 0.329" | awk '{print ($1 < $2)}'` == 1 ]]; then echo "Insufficient Accuracy" && exit 1;  fi
        echo "test.py script executed Successfully."
